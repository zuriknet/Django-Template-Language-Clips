Installation
------------

Double click the .clips file to install into Panic Coda.

Cheat Sheet
-----------

``tv + TAB`` -> template variable (``{{ ... }}``)
``# + TAB`` -> inline comment (``{# ... #}``)